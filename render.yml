services:
  - type: web
    name: anso-msf-validator
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: voila --port=$PORT --no-browser --Voila.ip=0.0.0.0 anso_msf_validator.ipynb
    autoDeploy: false